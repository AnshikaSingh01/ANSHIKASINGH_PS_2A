#include<stdio.h>
#include<string.h>
void main()
{
    char str[100],i,l,len;
    printf("enter the string");
    scanf("%c",&str);
    for(i=0;i!='\0';i++)
    {
        l++;
    }
     printf("length of the string is:",l);
}